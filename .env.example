# Postgres (Neon: pooled connection string em DATABASE_URL, direct em DIRECT_URL)
DATABASE_URL=""
DIRECT_URL=""
# Opcional — só necessário ao rodar contra `prisma dev` localmente, não em produção.
DATABASE_POOL_MAX=""

# Auth.js — gerar com `npx auth secret`
AUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Cloudinary
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
